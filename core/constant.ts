export const MEDIA_PATH = "/media";

export const MAX_TRANSCRIPT_LENGTH = 2000;

export enum LanguageCode {
  jaJP = "ja-JP",
  enUS = "en-US",
}

export enum Gender {
  male = "male",
  female = "female",
}
